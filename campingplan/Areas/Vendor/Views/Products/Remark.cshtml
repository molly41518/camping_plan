@using campingplan.Models;
@model campingplan.Models.product

@{
    ViewBag.Title = "商品描述";
    Layout = "~/Views/Shared/_LayoutEdit.cshtml";
}

@section scripts {
    <script src="~/Scripts/ckeditor/ckeditor.js"></script>
    <script>
        var editor = CKEDITOR.editor.replace(
            'remark' ,
            {
                skin: 'kama',
                width: '750px',
                filebrowserImageUploadUrl: '/Vendor/product/UploadImage'
            }
        );
    </script>
}

@using (Html.BeginForm("Remark", "product", new { area = "Vendor" }, FormMethod.Post, new { id = "popupForm" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.rowid)

    <div class="form-group">
        @Html.LabelFor(model => model.pno, htmlAttributes: new { @class = "control-label control-header" })
        @Html.Label("：", htmlAttributes: new { @class = "control-label control-header" })
        @if (Model.pno != null)
        {<span class="control-label control-value">@Model.pno</span>}
    </div>

    <div class="form-group">
        @Html.LabelFor(model => model.pname, htmlAttributes: new { @class = "control-label control-header" })
        @Html.Label("：", htmlAttributes: new { @class = "control-label control-header" })
        @if (Model.pname != null)
        {<span class="control-label control-value">@Model.pname</span>}
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.remark, htmlAttributes: new { @class = "control-label control-header" })
        @Html.TextArea("remark", new { id = "remark", @name = "remark" })
        @Html.ValidationMessageFor(model => model.remark)
    </div>
    <div>
        <input type="submit" value="存檔" class="btn btn-success form-button" />
    </div>
}