@model campingplan.Models.shippings

@{
    ViewBag.Title = "運送方式" + ((Model.rowid > 0) ? "修改" : "新增");
    Layout = "~/Views/Shared/_LayoutEdit.cshtml";
}

@using (Html.BeginForm("Edit", "Shipping", new { area = "Admin" }, FormMethod.Post, new { id = "popupForm" }))
{
    @Html.AntiForgeryToken()
    if (Model != null && Model.rowid > 0)
    {
        @Html.HiddenFor(model => model.rowid)
    }

    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(model => model.shipping_no, htmlAttributes: new { @class = "control-label control-header" })
        @Html.TextBoxFor(model => model.shipping_no, new { @class = "form-control" })
        @Html.ValidationMessageFor(model => model.shipping_no)
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.shipping_name, htmlAttributes: new { @class = "control-label control-header" })
        @Html.TextBoxFor(model => model.shipping_name, new { @class = "form-control" })
        @Html.ValidationMessageFor(model => model.shipping_name)
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.remark, htmlAttributes: new { @class = "control-label control-header" })
        @Html.TextBoxFor(model => model.remark, new { @class = "form-control" })
        @Html.ValidationMessageFor(model => model.remark)
    </div>

    <div>
        <input type="submit" value="存檔" class="btn btn-success form-button" />
    </div>
}